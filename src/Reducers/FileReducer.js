
const initialState = {
    Files: [],
    File: {}
};

export const FileReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'ACTION_TYPE':
            return 
        default:
            return state
    }
}